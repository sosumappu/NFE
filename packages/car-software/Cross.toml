[build]
toolchain = "stable"
[target.aarch64-unknown-linux-gnu]
pre-build = [
  "dpkg --add-architecture arm64",
  "apt-get update",
  "apt-get install -y libudev-dev:arm64 libfontconfig1-dev:arm64",
]
